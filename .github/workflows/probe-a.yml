name: Probe A
on:
  workflow_call:
    inputs:
      knoten:
        type: string
        default: "22"
jobs:
  bauen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A lebt, knoten=${{ inputs.knoten }}"
