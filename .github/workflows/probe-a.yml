name: Probe A
on:
  workflow_call:
    inputs:
      knoten: { type: string, default: "22" }
    secrets:
      MELDEN_URL: { required: false }
permissions:
  contents: write
jobs:
  bauen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A lebt, knoten=${{ inputs.knoten }}"
